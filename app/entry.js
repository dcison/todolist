import React from "react";
import './text.less';//引入样式
import ReactDOM from 'react-dom';
import Main from './components/main.js' //导入主组件


ReactDOM.render(
  <Main />,
  document.getElementById("container")
);